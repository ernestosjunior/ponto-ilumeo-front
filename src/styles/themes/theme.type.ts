export interface Theme {
  colors: {
    primary: string;
  };
  fontWeights: {
    light: string;
    regular: string;
    semibold: string;
    bold: string;
  };
  fontFamily: {
    primary: string;
  };
}
